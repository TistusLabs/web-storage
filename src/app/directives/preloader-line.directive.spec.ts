import { PreloaderLineDirective } from './preloader-line.directive';

describe('PreloaderLineDirective', () => {
  it('should create an instance', () => {
    const directive = new PreloaderLineDirective();
    expect(directive).toBeTruthy();
  });
});
